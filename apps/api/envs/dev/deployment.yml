---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shieldapi-deployment
spec:
  template:
    spec:
      containers:
        - name: shield-api
          env:
            - name: Environment
              value: "dev"
            - name: EnvironmentName
              value: "shielddev1"